
  
    grant usage on schema dbt_jkolpak to role reporter;
    grant select on all tables in schema dbt_jkolpak to role reporter;
    grant select on all views in schema dbt_jkolpak to role reporter;
    grant select on future tables in schema dbt_jkolpak to role reporter;
    grant select on future views in schema dbt_jkolpak to role reporter;
  
